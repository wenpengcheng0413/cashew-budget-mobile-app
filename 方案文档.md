# iOS 记账 App — 完整开发与部署方案

## 当前状态

基于 **Cashew** 开源项目（1500+ Stars），已完成以下定制：

| 功能 | 状态 |
|------|------|
| CSV 导入 | ✅ 原项目支持（列映射、日期格式识别、批量导入） |
| CSV 导出 | ✅ 原项目支持（按时间范围/账户筛选导出） |
| Excel(.xlsx) 导入 | ✅ **新增**（解析 .xlsx/.xls 文件，复用 CSV 导入流程） |
| Excel(.xlsx) 导出 | ✅ **新增**（多 Sheet：交易明细 + 分类汇总 + 月度统计） |
| 中文界面 | ✅ 已有完整中文翻译（zh.json, 1183条） |
| Firebase 云同步 | ✅ 原项目支持（免费 Spark 计划足够） |
| 本地 SQLite | ✅ 使用 drift ORM |
| Codemagic CI/CD | ✅ 配置完成 |

## 项目位置

```
D:\py_code\测试记账app\
├── budget/              ← Flutter 项目（Cashew）
│   ├── lib/
│   │   ├── widgets/
│   │   │   ├── importCSV.dart      ← 原有 CSV 导入
│   │   │   ├── exportCSV.dart      ← 原有 CSV 导出
│   │   │   ├── importExcel.dart    ← 🆕 Excel 导入
│   │   │   └── exportExcel.dart    ← 🆕 Excel 导出
│   │   ├── pages/
│   │   │   └── settingsPage.dart   ← 已添加 Excel 导入/导出入口
│   │   └── ...
│   ├── ios/                        ← iOS 配置
│   ├── codemagic.yaml              ← 🆕 CI/CD 配置
│   ├── FIREBASE_SETUP.md           ← 🆕 Firebase 配置指南
│   └── pubspec.yaml               ← 已添加 excel 依赖
├── 方案文档.md
└── .git/
```

## 完整开发者工作流程

### 一、本地开发（Windows）
```bash
# 进入项目目录
cd D:\py_code\测试记账app\budget

# 安装依赖
flutter pub get

# （修改代码后）重新生成数据库代码
dart run build_runner build --delete-conflicting-outputs
```

### 二、推送到 GitHub
```bash
git add .
git commit -m "feat: 添加 Excel 导入导出和中文支持"
git push origin main
```

### 三、Codemagic 云构建 IPA

#### 前置准备：
1. 注册 [Codemagic](https://codemagic.io)（GitHub 登录）
2. 连接你的 GitHub 仓库
3. 在 Codemagic 中添加 Apple ID（免费账号即可）：
   - Team → Code Signing → Add Apple ID
   - 输入你的 Apple ID 和密码

#### 构建：
1. 推送代码到 main 分支 → 自动触发构建
2. 或手动触发构建
3. 构建完成后从 Artifacts 下载 `.ipa` 文件

#### 免费额度：
- Codemagic: 500 分钟/月（足够每月构建几十次）
- Firebase: Spark 免费计划（1GB 存储 + 5万次读取/天）

### 四、Firebase 配置（云同步）

1. 在 [Firebase Console](https://console.firebase.google.com) 创建项目
2. 注册 iOS 应用（Bundle ID: `com.budget.tracker-app`）
3. 下载 `GoogleService-Info.plist` 放到 `budget/ios/Runner/`
4. 启用 Firestore Database 和 Authentication
5. 运行 `firebase login` + `flutterfire configure`

> 详细步骤见 `budget/FIREBASE_SETUP.md`

### 五、侧载到 iPhone（Sideloadly）

1. 下载并安装 [Sideloadly](https://sideloadly.io/)（Windows 版）
2. 用 USB 连接 iPhone
3. 打开 Sideloadly → 拖入 IPA 文件
4. 输入你的 Apple ID（免费账号）
5. 点击 Start → 自动签名安装

#### 重要说明：
- **免费 Apple ID 限制**：签名 7 天过期，需要在过期前重新连接电脑重签
- **Sideloadly 会自动重签**：只需在 7 天内重新运行一次 Sideloadly
- **App 数量限制**：免费账号最多签 3 个 App

## 新增功能说明

### Excel 导入
- 支持 `.xlsx` 和 `.xls` 格式
- 自动识别列名（Date, Amount, Category, Title, Note, Account 等）
- 智能日期格式解析
- 自动创建不存在的分类和账户
- 批量导入，显示进度

### Excel 导出
- 包含 3 个 Sheet：
  1. **Transaction Details** — 所有交易明细
  2. **Category Summary** — 按分类汇总统计
  3. **Monthly Summary** — 按月份汇总收入/支出/净额
- 支持按账户筛选和时间范围筛选
- 带格式的 Excel 文件（表头加粗、颜色标记）

## 注意事项

1. **iOS 构建必须用 macOS** → 通过 Codemagic 云构建解决
2. **Firebase 免费额度完全够用** → 个人记账月均几百条记录，远低于免费额度
3. **GoogleService-Info.plist** → 必须从 Firebase Console 下载放到 ios/Runner/
4. **Bundle ID 不能随意改** → 需与 Firebase 和 Apple 开发者配置一致
5. **签名 7 天过期** → Sideloadly 每 7 天重签一次即可

## 全部命令速查

```bash
# 本地开发
cd D:\py_code\测试记账app\budget
flutter pub get                                    # 安装依赖
dart run build_runner build --delete-conflicting-outputs  # 生成数据库代码

# 推送到 GitHub
git add . && git commit -m "你的提交信息" && git push origin main

# 构建 IPA（在 Codemagic 上自动执行，也可以本地 macOS 执行）
flutter build ipa --release

# 侧载到 iPhone
# 1. 打开 Sideloadly
# 2. 拖入 .ipa 文件
# 3. 输入 Apple ID → Start
```
